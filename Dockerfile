# ---------------------------------------------------------------
# ベースイメージ
# Cloud Run推奨のslimイメージを使用
# ---------------------------------------------------------------
FROM python:3.12-slim

# ---------------------------------------------------------------
# 環境変数
# ---------------------------------------------------------------
# Pythonの出力バッファリングを無効化（Cloud Runのログをリアルタイム出力）
ENV PYTHONUNBUFFERED=1
# .pycファイルの生成を抑制
ENV PYTHONDONTWRITEBYTECODE=1
# pipキャッシュを無効化（イメージサイズ削減）
ENV PIP_NO_CACHE_DIR=1

# ---------------------------------------------------------------
# 作業ディレクトリ
# ---------------------------------------------------------------
WORKDIR /app

# ---------------------------------------------------------------
# 依存パッケージのインストール
# requirements.txtを先にコピーすることでDockerキャッシュを活用
# ---------------------------------------------------------------
COPY requirements.txt .
RUN pip install --upgrade pip && \
    pip install -r requirements.txt

# ---------------------------------------------------------------
# アプリケーションコードのコピー
# ---------------------------------------------------------------
COPY main.py .
COPY servers/ ./servers/

# ---------------------------------------------------------------
# ポート公開
# Cloud RunはPORT環境変数でポートを動的に指定する
# ---------------------------------------------------------------
EXPOSE 8080

# ---------------------------------------------------------------
# 起動コマンド
# main.pyのif __name__ == "__main__"ブロックを実行
# ---------------------------------------------------------------
CMD ["python", "main.py"]